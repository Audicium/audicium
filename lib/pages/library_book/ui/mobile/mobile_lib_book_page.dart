import 'package:flutter/material.dart';

class MobileLibraryBookPage extends StatelessWidget {
  const MobileLibraryBookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('You are browsing saved book'),
    );
  }
}
